---
phase: 02-http-client-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/tenable.ts
  - src/utils/error-handling.ts
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - ERR-01
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "All Tenable.io API response shapes have typed interfaces (no `any` in API call paths)"
    - "Scan ID is typed as number and scan UUID as string at the abstraction boundary"
    - "NessusErrorType enum includes RATE_LIMITED, AUTH_FAILED, and TIMEOUT values"
    - "HTTP 409 maps to SCAN_IN_PROGRESS error type"
    - "createNessusError handles all new error types and maps them to correct MCP error codes"
  artifacts:
    - path: "src/types/tenable.ts"
      provides: "All Tenable.io API response interfaces"
      exports:
        - "ScanId"
        - "ScanUuid"
        - "TenableScanListResponse"
        - "TenableScanDetailsResponse"
        - "TenableTemplateListResponse"
        - "TenableCreateScanResponse"
        - "TenableLaunchScanResponse"
        - "TenableWorkbenchVulnsResponse"
    - path: "src/utils/error-handling.ts"
      provides: "Extended NessusErrorType enum and httpStatusToErrorType mapper"
      exports:
        - "NessusErrorType"
        - "createNessusError"
        - "handleNessusApiError"
        - "httpStatusToErrorType"
  key_links:
    - from: "src/utils/error-handling.ts"
      to: "@modelcontextprotocol/sdk/types.js"
      via: "McpError and ErrorCode imports"
      pattern: "import.*ErrorCode.*McpError"
---

<objective>
Create the type definitions and error infrastructure that the TenableClient (Plan 02) and all tool handlers (Phase 3) depend on.

Purpose: Establish typed contracts for every Tenable.io API response and extend the error system with HTTP-specific error types. This is the foundation layer -- nothing else can be built type-safely without it.
Output: `src/types/tenable.ts` with all response interfaces, updated `src/utils/error-handling.ts` with new error types and HTTP status mapper.
</objective>

<execution_context>
@/home/c_perronnet/.claude/get-shit-done/workflows/execute-plan.md
@/home/c_perronnet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-http-client-and-infrastructure/02-RESEARCH.md

<interfaces>
<!-- Existing error-handling.ts exports that will be extended -->
From src/utils/error-handling.ts:
```typescript
export enum NessusErrorType {
  SCAN_NOT_FOUND = 'scan_not_found',
  VULNERABILITY_NOT_FOUND = 'vulnerability_not_found',
  INVALID_SCAN_TYPE = 'invalid_scan_type',
  INVALID_TARGET = 'invalid_target',
  SCAN_IN_PROGRESS = 'scan_in_progress',
  API_ERROR = 'api_error',
  CONFIGURATION_ERROR = 'configuration_error'
}

export const createNessusError = (type: NessusErrorType, message: string, details?: unknown): McpError => { ... };
export const handleNessusApiError = (error: unknown): McpError => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tenable.io response type interfaces</name>
  <files>src/types/tenable.ts</files>
  <action>
Create `src/types/tenable.ts` with typed interfaces for all Tenable.io API responses. Use the exact field names from the research document (sourced from Tenable Python/Java SDKs).

Define these type aliases and interfaces:

1. `ScanId = number` and `ScanUuid = string` type aliases (TYPE-02)
2. `TenableFolder` — id, name, type, default_tag, custom, unread_count
3. `TenableScan` — id (ScanId), uuid (ScanUuid), name, type, owner, enabled, folder_id, read, status, shared, user_permissions, creation_date (number, Unix epoch), last_modification_date (number, Unix epoch)
4. `TenableScanListResponse` — folders (TenableFolder[]), scans (TenableScan[] | null — null when account has no scans), timestamp (number)
5. `TenableScanInfo` — status, name, uuid (ScanUuid), folder_id, targets, scan_start (string | null), scan_end (string | null), scanner_name, hostcount, user_permissions, policy, scan_type, object_id, tag_targets (string[]), acls (unknown[])
6. `TenableScanHost` — host_id, host_index, hostname, progress, critical, high, medium, low, info, totalchecksconsidered, numchecksconsidered, scanprogresstotal, scanprogresscurrent, score
7. `TenableScanVulnerability` — plugin_id, plugin_name, plugin_family, count, vuln_index, severity_index, severity (number: 0=info, 1=low, 2=medium, 3=high, 4=critical)
8. `TenableScanHistory` — history_id, uuid (ScanUuid), owner_id, status, creation_date, last_modification_date
9. `TenableScanDetailsResponse` — info (TenableScanInfo), hosts (TenableScanHost[]), vulnerabilities (TenableScanVulnerability[]), compliance (unknown[]), history (TenableScanHistory[]), notes (unknown), remediations (unknown), filters (unknown[])
10. `TenableTemplate` — uuid (string), name, title, description, cloud_only (boolean), subscription_only (boolean), is_agent (boolean), more_info (optional string)
11. `TenableTemplateListResponse` — templates (TenableTemplate[])
12. `TenableCreateScanResponse` — scan object with id (ScanId), uuid (ScanUuid), name, type, owner, enabled, folder_id, creation_date, last_modification_date
13. `TenableLaunchScanResponse` — scan_uuid (ScanUuid)
14. `TenableWorkbenchVulnerability` — plugin_id, plugin_name, plugin_family, severity (number), vpr_score (optional number), count
15. `TenableWorkbenchVulnsResponse` — vulnerabilities (TenableWorkbenchVulnerability[]), total_vulnerability_count (number)

All interfaces and types must be exported. No `any` types anywhere (TYPE-01). Use `unknown` for fields whose shape is not yet confirmed. All local imports must use `.js` extension per project convention.
  </action>
  <verify>
    <automated>cd /home/c_perronnet/git/monitoring/netbox/tools/nessus-mcp-server && npx tsc --noEmit src/types/tenable.ts</automated>
  </verify>
  <done>src/types/tenable.ts exists with all 15+ exported interfaces/types, zero `any` types, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Extend error types and add HTTP status mapper</name>
  <files>src/utils/error-handling.ts</files>
  <action>
Modify `src/utils/error-handling.ts` to:

1. Add three new values to the `NessusErrorType` enum (ERR-01):
   - `RATE_LIMITED = 'rate_limited'`
   - `AUTH_FAILED = 'auth_failed'`
   - `TIMEOUT = 'timeout'`

2. Update the `createNessusError` switch statement to handle the new types:
   - `RATE_LIMITED` -> `ErrorCode.InternalError` (transient, will be retried)
   - `AUTH_FAILED` -> `ErrorCode.InvalidParams` (bad credentials are a config problem)
   - `TIMEOUT` -> `ErrorCode.InternalError` (transient)
   - `SCAN_IN_PROGRESS` already exists in the enum but ensure it maps to `ErrorCode.InvalidRequest` (ERR-02: HTTP 409)

3. Add a new exported function `httpStatusToErrorType(status: number): NessusErrorType` that maps HTTP status codes to error types (ERR-03):
   - 401 -> AUTH_FAILED
   - 403 -> AUTH_FAILED
   - 404 -> SCAN_NOT_FOUND
   - 409 -> SCAN_IN_PROGRESS (ERR-02)
   - 429 -> RATE_LIMITED
   - 5xx (>= 500) -> API_ERROR
   - default -> API_ERROR

4. Update `handleNessusApiError` to detect timeout errors: if `error instanceof DOMException && error.name === 'TimeoutError'`, return a TIMEOUT error. If `error instanceof DOMException && error.name === 'AbortError'`, return an API_ERROR with "Request was aborted" message. This ensures all error paths return structured MCP error responses (ERR-03).

Keep all existing validation functions (`validateScanId`, `validateVulnerabilityId`, `validateTarget`, `validateScanType`) unchanged.
  </action>
  <verify>
    <automated>cd /home/c_perronnet/git/monitoring/netbox/tools/nessus-mcp-server && npx tsc --noEmit src/utils/error-handling.ts</automated>
  </verify>
  <done>NessusErrorType has RATE_LIMITED, AUTH_FAILED, TIMEOUT values; httpStatusToErrorType function exported; handleNessusApiError handles DOMException timeout/abort; all compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors across the entire project
- `src/types/tenable.ts` exports ScanId, ScanUuid, and all response interfaces
- `src/utils/error-handling.ts` exports httpStatusToErrorType function
- No `any` types in either file (grep confirms)
</verification>

<success_criteria>
- TYPE-01: All Tenable.io response shapes have typed interfaces in src/types/tenable.ts
- TYPE-02: ScanId is number, ScanUuid is string, both exported as type aliases
- ERR-01: NessusErrorType enum has RATE_LIMITED, AUTH_FAILED, TIMEOUT
- ERR-02: HTTP 409 maps to SCAN_IN_PROGRESS via httpStatusToErrorType
- ERR-03: handleNessusApiError catches DOMException timeout/abort and returns structured MCP errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-http-client-and-infrastructure/02-01-SUMMARY.md`
</output>
